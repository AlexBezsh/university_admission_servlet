package com.bezshtanko.university_admission_servlet.controller.command.get;

import com.bezshtanko.university_admission_servlet.controller.command.Command;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class FacultyEnrollGet implements Command {
    @Override
    public void execute(HttpServletRequest request, HttpServletResponse response, String templatesPath, String templatesSuffix) {
    }
}
